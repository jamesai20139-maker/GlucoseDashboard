# Backend contract test location

Contract tests for CLI and local-service operations live here. The first executable
coverage is provided by the unit tests in `src/ingestion` and `src/analysis`; this
directory is intentionally kept ready for endpoint-level tests.
